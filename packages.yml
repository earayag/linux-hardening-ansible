- name: Install basic packages
  hosts: all
  become: yes

  tasks:
    - name: Install common packages
      package:
        name:
          - vim
          - curl
          - wget
        state: present
